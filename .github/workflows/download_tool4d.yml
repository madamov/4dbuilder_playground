name: Download and cache tool4d

on:
  workflow_call:
    inputs:
      version:
        description: "4D version, for example: 21.1, 20.8, or 20.8 HF3"
        required: true
        type: string

      runner:
        description: "Runner on which tool4d will be downloaded"
        required: false
        type: string
        default: "macos-latest"

      versions-file:
        description: "Path to the JSON file containing available versions"
        required: false
        type: string
        default: ".github/tool4d-versions.json"

    outputs:
      url:
        description: "Resolved tool4d download URL"
        value: ${{ jobs.download.outputs.url }}

      build:
        description: "Selected build number or hotfix"
        value: ${{ jobs.download.outputs.build }}

      os:
        description: "Resolved 4D operating-system identifier"
        value: ${{ jobs.download.outputs.os }}

      target:
        description: "Resolved 4D architecture identifier"
        value: ${{ jobs.download.outputs.target }}

      cache-key:
        description: "Cache key used for the extracted tool4d installation"
        value: ${{ jobs.download.outputs.cache-key }}

      cache-hit:
        description: "Whether tool4d was restored from cache"
        value: ${{ jobs.download.outputs.cache-hit }}

      install-path:
        description: "Path containing the extracted tool4d files"
        value: ${{ jobs.download.outputs.install-path }}

jobs:
  download:
    name: Download tool4d ${{ inputs.version }}
    runs-on: ${{ inputs.runner }}

    outputs:
      url: ${{ steps.resolve.outputs.url }}
      build: ${{ steps.resolve.outputs.build }}
      os: ${{ steps.resolve.outputs.os }}
      target: ${{ steps.resolve.outputs.target }}
      cache-key: ${{ steps.resolve.outputs.cache_key }}
      cache-hit: ${{ steps.cache.outputs.cache-hit }}
      install-path: ${{ steps.resolve.outputs.install_path }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Resolve platform, build and URL
        id: resolve
        shell: bash
        env:
          REQUESTED_VERSION: ${{ inputs.version }}
          VERSIONS_FILE: ${{ inputs.versions-file }}
          RUNNER_OS_VALUE: ${{ runner.os }}
          RUNNER_ARCH_VALUE: ${{ runner.arch }}
        run: |
          set -euo pipefail

          echo "Requested version  : $REQUESTED_VERSION"
          echo "Runner OS          : $RUNNER_OS_VALUE"
          echo "Runner architecture: $RUNNER_ARCH_VALUE"

          if [[ ! -f "$VERSIONS_FILE" ]]; then
            echo "::error::Versions file does not exist: $VERSIONS_FILE"
            exit 1
          fi

          case "$RUNNER_OS_VALUE" in
            macOS)
              TOOL4D_OS="mac"

              case "$RUNNER_ARCH_VALUE" in
                ARM64)
                  TOOL4D_TARGET="arm64"
                  TOOL4D_ARCHIVE="tool4d_arm64.tar.xz"
                  ;;

                X64)
                  TOOL4D_TARGET="x86_64"
                  TOOL4D_ARCHIVE="tool4d_x86_64.tar.xz"
                  ;;

                *)
                  echo "::error::Unsupported macOS architecture: $RUNNER_ARCH_VALUE"
                  exit 1
                  ;;
              esac
              ;;

            Windows)
              TOOL4D_OS="win"
              TOOL4D_TARGET="x86_64"
              TOOL4D_ARCHIVE="tool4d_win.tar.xz"
              ;;

            *)
              echo "::error::Unsupported operating system: $RUNNER_OS_VALUE"
              echo "::error::Only Windows and macOS are supported."
              exit 1
              ;;
          esac

          SELECTED_ENTRY="$(
            jq -c \
              --arg version "$REQUESTED_VERSION" \
              --arg os "$TOOL4D_OS" \
              --arg target "$TOOL4D_TARGET" \
              '
              [
                .[]
                | select(
                    .version == $version
                    and .os == $os
                    and .target == $target
                  )
                | . + {
                    "_build_sort":
                      if (.build | test("^[0-9]+$"))
                      then (.build | tonumber)
                      else -1
                      end
                  }
              ]
              | sort_by(._build_sort)
              | last
              | if . == null
                then null
                else del(._build_sort)
                end
              ' \
              "$VERSIONS_FILE"
          )"

          if [[ "$SELECTED_ENTRY" == "null" || -z "$SELECTED_ENTRY" ]]; then
            echo "::error::No matching tool4d version was found."
            echo "::error::Version: $REQUESTED_VERSION"
            echo "::error::OS: $TOOL4D_OS"
            echo "::error::Target: $TOOL4D_TARGET"

            echo "Entries available for version $REQUESTED_VERSION:"

            jq \
              --arg version "$REQUESTED_VERSION" \
              '[.[] | select(.version == $version)]' \
              "$VERSIONS_FILE"

            exit 1
          fi

          BUILD="$(jq -r '.build' <<< "$SELECTED_ENTRY")"

          if [[ "$REQUESTED_VERSION" =~ ^([0-9]+)\. ]]; then
            MAJOR_VERSION="${BASH_REMATCH[1]}"
          else
            echo "::error::Invalid version format: $REQUESTED_VERSION"
            exit 1
          fi

          VERSION_PATH="${REQUESTED_VERSION// /%20}"

          URL="https://product-download.4d.com/release/${MAJOR_VERSION}.x/${VERSION_PATH}/${BUILD}/${TOOL4D_OS}/${TOOL4D_ARCHIVE}"

          # Make the version safe for use in folder names and cache keys.
          SAFE_VERSION="$(
            printf '%s' "$REQUESTED_VERSION" |
              tr '[:upper:]' '[:lower:]' |
              sed -E 's/[^a-z0-9._-]+/-/g'
          )"

          CACHE_KEY="tool4d-${SAFE_VERSION}-${BUILD}-${TOOL4D_OS}-${TOOL4D_TARGET}"

          # RUNNER_TOOL_CACHE is available on GitHub-hosted runners.
          INSTALL_PATH="${RUNNER_TOOL_CACHE}/tool4d/${SAFE_VERSION}/${BUILD}/${TOOL4D_OS}-${TOOL4D_TARGET}"

          echo "Selected entry:"
          jq . <<< "$SELECTED_ENTRY"

          echo "Resolved URL : $URL"
          echo "Cache key    : $CACHE_KEY"
          echo "Install path : $INSTALL_PATH"

          {
            echo "url=$URL"
            echo "build=$BUILD"
            echo "os=$TOOL4D_OS"
            echo "target=$TOOL4D_TARGET"
            echo "archive=$TOOL4D_ARCHIVE"
            echo "cache_key=$CACHE_KEY"
            echo "install_path=$INSTALL_PATH"
          } >> "$GITHUB_OUTPUT"

      - name: Restore tool4d cache
        id: cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.resolve.outputs.install_path }}
          key: ${{ steps.resolve.outputs.cache_key }}

      - name: Download tool4d archive
        if: steps.cache.outputs.cache-hit != 'true'
        id: download
        shell: bash
        env:
          DOWNLOAD_URL: ${{ steps.resolve.outputs.url }}
          ARCHIVE_NAME: ${{ steps.resolve.outputs.archive }}
          PRODUCT_DOWNLOAD_USERNAME: ${{ secrets.PRODUCT_DOWNLOAD_USERNAME }}
          PRODUCT_DOWNLOAD_PASSWORD: ${{ secrets.PRODUCT_DOWNLOAD_PASSWORD }}
        run: |
          set -euo pipefail

          DOWNLOAD_DIRECTORY="$RUNNER_TEMP/tool4d-download"
          ARCHIVE_PATH="$DOWNLOAD_DIRECTORY/$ARCHIVE_NAME"

          rm -rf "$DOWNLOAD_DIRECTORY"
          mkdir -p "$DOWNLOAD_DIRECTORY"

          CURL_ARGUMENTS=(
            --fail
            --location
            --show-error
            --silent
            --retry 3
            --retry-delay 5
            --output "$ARCHIVE_PATH"
          )

          if [[ -n "$PRODUCT_DOWNLOAD_USERNAME" || -n "$PRODUCT_DOWNLOAD_PASSWORD" ]]; then
            if [[ -z "$PRODUCT_DOWNLOAD_USERNAME" || -z "$PRODUCT_DOWNLOAD_PASSWORD" ]]; then
              echo "::error::Both PRODUCT_DOWNLOAD_USERNAME and PRODUCT_DOWNLOAD_PASSWORD must be provided."
              exit 1
            fi

            CURL_ARGUMENTS+=(
              --user
              "${PRODUCT_DOWNLOAD_USERNAME}:${PRODUCT_DOWNLOAD_PASSWORD}"
            )
          fi

          echo "Downloading:"
          echo "$DOWNLOAD_URL"

          curl "${CURL_ARGUMENTS[@]}" "$DOWNLOAD_URL"

          if [[ ! -s "$ARCHIVE_PATH" ]]; then
            echo "::error::Downloaded archive is missing or empty: $ARCHIVE_PATH"
            exit 1
          fi

          echo "archive_path=$ARCHIVE_PATH" >> "$GITHUB_OUTPUT"

          ls -lh "$ARCHIVE_PATH"

      - name: Extract tool4d archive
        if: steps.cache.outputs.cache-hit != 'true'
        shell: bash
        env:
          ARCHIVE_PATH: ${{ steps.download.outputs.archive_path }}
          INSTALL_PATH: ${{ steps.resolve.outputs.install_path }}
        run: |
          set -euo pipefail

          rm -rf "$INSTALL_PATH"
          mkdir -p "$INSTALL_PATH"

          tar -xJf "$ARCHIVE_PATH" -C "$INSTALL_PATH"

          echo "Extracted tool4d files:"
          find "$INSTALL_PATH" -maxdepth 3 -type f -print

          if [[ "${RUNNER_OS}" == "macOS" ]]; then
            TOOL4D_EXECUTABLE="$(
              find "$INSTALL_PATH" \
                -type f \
                -name "tool4d" \
                -print \
                -quit
            )"
          else
            TOOL4D_EXECUTABLE="$(
              find "$INSTALL_PATH" \
                -type f \
                -iname "tool4d.exe" \
                -print \
                -quit
            )"
          fi

          if [[ -z "$TOOL4D_EXECUTABLE" ]]; then
            echo "::error::tool4d executable was not found after extraction."
            exit 1
          fi

          chmod +x "$TOOL4D_EXECUTABLE" 2>/dev/null || true

          echo "tool4d executable: $TOOL4D_EXECUTABLE"

      - name: Verify cached tool4d
        shell: bash
        env:
          INSTALL_PATH: ${{ steps.resolve.outputs.install_path }}
        run: |
          set -euo pipefail

          if [[ ! -d "$INSTALL_PATH" ]]; then
            echo "::error::tool4d installation directory does not exist: $INSTALL_PATH"
            exit 1
          fi

          if [[ "${RUNNER_OS}" == "macOS" ]]; then
            TOOL4D_EXECUTABLE="$(
              find "$INSTALL_PATH" \
                -type f \
                -name "tool4d" \
                -print \
                -quit
            )"
          else
            TOOL4D_EXECUTABLE="$(
              find "$INSTALL_PATH" \
                -type f \
                -iname "tool4d.exe" \
                -print \
                -quit
            )"
          fi

          if [[ -z "$TOOL4D_EXECUTABLE" ]]; then
            echo "::error::tool4d executable was not found in: $INSTALL_PATH"
            exit 1
          fi

          chmod +x "$TOOL4D_EXECUTABLE" 2>/dev/null || true

          TOOL4D_DIRECTORY="$(dirname "$TOOL4D_EXECUTABLE")"

          echo "TOOL4D_HOME=$INSTALL_PATH" >> "$GITHUB_ENV"
          echo "TOOL4D_EXECUTABLE=$TOOL4D_EXECUTABLE" >> "$GITHUB_ENV"
          echo "$TOOL4D_DIRECTORY" >> "$GITHUB_PATH"

          echo "Cache hit          : ${{ steps.cache.outputs.cache-hit }}"
          echo "tool4d installation: $INSTALL_PATH"
          echo "tool4d executable  : $TOOL4D_EXECUTABLE"

      - name: Add result to job summary
        shell: bash
        env:
          URL: ${{ steps.resolve.outputs.url }}
          BUILD: ${{ steps.resolve.outputs.build }}
          TOOL4D_OS: ${{ steps.resolve.outputs.os }}
          TOOL4D_TARGET: ${{ steps.resolve.outputs.target }}
          CACHE_KEY: ${{ steps.resolve.outputs.cache_key }}
          INSTALL_PATH: ${{ steps.resolve.outputs.install_path }}
          CACHE_HIT: ${{ steps.cache.outputs.cache-hit }}
        run: |
          {
            echo "## tool4d download"
            echo
            echo "| Property | Value |"
            echo "|---|---|"
            echo "| Version | \`${{ inputs.version }}\` |"
            echo "| Build | \`$BUILD\` |"
            echo "| OS | \`$TOOL4D_OS\` |"
            echo "| Target | \`$TOOL4D_TARGET\` |"
            echo "| Cache key | \`$CACHE_KEY\` |"
            echo "| Cache hit | \`$CACHE_HIT\` |"
            echo "| Install path | \`$INSTALL_PATH\` |"
            echo "| URL | $URL |"
          } >> "$GITHUB_STEP_SUMMARY"
          